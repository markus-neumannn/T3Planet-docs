---
description: T3Planet Mintlify deployment — which GitHub remote updates live docs
alwaysApply: true
---

# Mintlify deployment (T3Planet Docs)

Live site: https://docs.t3planet.de/en/latest  
Mintlify project: **T3Planet Docs** (workspace **nitsan-81630f36**)

## Connected GitHub (source of live builds)

**Use only the org repository** for deployment:

- Live / deploy source: **`nitsan-technologies/T3Planet-docs`** branch `master`
- Local remote name: **`origin`** → `https://github.com/nitsan-technologies/T3Planet-docs.git`

Do **not** use `markus-neumannn/T3Planet-docs` for deployment.

Mintlify dashboard **Git** settings must point at **`nitsan-technologies/T3Planet-docs`** (`master`). If Git settings still show the Markus fork, live will not follow `origin` pushes — reconnect Git in the Mintlify dashboard.

For releases that must go live:

1. Commit as **Markus** `<248457632+markus-neumannn@users.noreply.github.com>` (do not invent email).
2. `git push origin HEAD:master` (required for Mintlify when Git is connected to the org repo).
3. Confirm Mintlify dashboard **Activity** + spot-check the live URL.

Full procedure: see repo root `context.md`.

## Local tooling

- Node **20** for `mintlify` / `mint` (Node 25+ unsupported).
- After nav/page count changes: `python3 scripts/compute_doc_stats.py`.
- Do not commit `RST Format */` or `scripts/remigration/*` QA dumps unless explicitly requested.
- Do not commit or push `workshops/` or `docs-master/`.
