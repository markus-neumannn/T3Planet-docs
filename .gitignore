.DS_Store
node_modules/
.mintlify/
preview-*.png
.venv-translate/
scripts/.de_cache.json
.review-frames/
scripts/dark_mode_audit_run.log

# Visual regression fixtures/results — not part of Mintlify publish set
visual-regression/

# Local Sphinx clone / legacy build — never publish (bloated pages + huge tree)
Live-docs/
!docs/deployment/
!docs/deployment/**
docs/
docs/_build/
docs-master/

# Local workshop PPT/site — never deploy
workshops/
